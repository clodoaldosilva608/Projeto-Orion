# Variável de ambiente do banco de dados

# SQLite (desenvolvimento local)
DATABASE_URL="file:./db/custom.db"

# PostgreSQL (produção - recomendado para nuvem)
# DATABASE_URL="postgresql://username:password@host:5432/orion?schema=public"

# MySQL (alternativa)
# DATABASE_URL="mysql://username:password@host:3306/orion"
